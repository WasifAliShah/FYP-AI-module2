# Core dependencies
numpy>=1.21.0
opencv-python>=4.5.0
opencv-contrib-python>=4.5.0

# PyTorch and related
torch>=1.12.0
torchvision>=0.13.0

# YOLO (Ultralytics)
ultralytics>=8.0.0

# Face Recognition
insightface>=0.7.3
onnxruntime>=1.12.0

# ReID (Person Re-identification)
torchreid>=1.4.0
gdown>=4.0.0  # Required dependency for torchreid

# Multi-object Tracking
cjm-byte-track>=0.0.6  # ByteTrack for robust multi-object tracking

# Optional: Real-ESRGAN for super-resolution
# Uncomment if you want to use Real-ESRGAN
# realesrgan>=0.3.0
# basicsr>=1.4.2
# facexlib>=0.3.0
# gfpgan>=1.3.8

# Additional utilities (if needed)
# Pillow>=9.0.0  # Usually comes with torchvision
# scipy>=1.7.0  # Sometimes needed for some ML operations

# Note: 
# - Model files (*.pt, *.onnx) should be downloaded separately or provided
# - InsightFace models will be downloaded automatically on first run
# - For GPU support, install PyTorch with CUDA: https://pytorch.org/get-started/locally/

